# Ignore old folders not needed for the live site
OLD_VERSION/
HTMLs/
CSVs/
PYTHON/
JAVA_SCRIPT/
docs/
src/
versions/

# OS files
.DS_Store
Thumbs.db

# Temp files
*.tmp.*

# Auto Claude data directory
.auto-claude/

# Secrets - NEVER commit these
*CREDENTIALS*.txt
*credentials*.txt
CLAUDE_DATABASE_ACCESS.txt
PASSWORD_BACKUP.txt
PASSWORD*.txt
*.env
.env.*

# Local-only build artifacts / backups (not for the public repo)
*_OLD.html
*_OLD.js
BACKUP_*/
*BACKUP*/
GITHUB_COPY_*/
UPDATED_LOCAL_REPO_*/
TEMP_GITHUB_*/
_V1_ARCHIVE_STAGING/
Auto-Claude/
*.exe
QUESTIONS____________________________/
# Working conversation between Shir and Claude: reports, questions, her answers.
# Local only - these are notes about the project, not part of the published site.
CONVERSATIONS/
# PARTICIPANT DATA. Weekly pull of the Supabase results tables. This repo is
# PUBLIC - research data must never be published to it, however anonymous the
# rows look. The script that writes this (TOOLS/weekly_supabase_backup.py) is
# tracked; what it downloads is not.
DATA_BACKUP/
AGENTS/
AUTO_CLAUDE_AGENTS/
CGPT/
DOCX/
PDFs/
PRESENTATIONS/
REFRENCES/
FIGURES/
FRAMES-FROM-VIDEO*/
SUBMISSION*/
In_Practice*/
QUESTIONS*/
*.docx
*.pdf
*.mp4
*.ai
*.tif
*.jpg
nul

# Personal application documents. This repository is public - GitHub Pages
# serves it - and these carry a home address, a phone number and referee
# details. *.docx and *.pdf above already cover the rendered files, but not
# the HTML masters they are built from.
POST_DOC_APPLICATION/
